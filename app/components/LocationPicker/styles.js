import { Platform, StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        marginTop: 10
    },
    dimensions: {
        height: 100,
        width: 150
    }
});

export { styles }
