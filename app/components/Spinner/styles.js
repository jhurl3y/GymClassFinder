import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    alignItems: "center",
    marginTop: 150
  },
  activityIndicator: {
    height: 80
  }
});

export { styles };
